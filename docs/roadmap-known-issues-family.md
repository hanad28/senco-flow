# 🗺️ Roadmap & Known Issues — Family

> Notion export — 2026-08-03. Source: child page of `Build Log`.
> This page is currently an unfilled template in Notion — exported as-is so the file structure mirrors the workspace. Update both in Notion and here as the family party-view roadmap firms up.

What's next to build, what's cut for speed and needs revisiting, and the future backlog — **family party-view**. Cross-platform items live in `buildlog.md`; SENDCO-specific items live in `roadmap-known-issues-sendco.md`.

---

## Build sequencing

*(Log the phased build plan here once the narrow first wedge is scoped. Per the family-side kickoff prompt: audit Husaam's existing build first, produce a gap list, then scope the single narrowest wedge — don't attempt the full Idea 5 feature list at once — then build phase by phase with the same verify-everything discipline as the SENDCO build.)*

---

## Known issues & debt

*(Log things cut for speed that need to be revisited, immediately as they're found — not just mentioned in chat.)*

---

## Future roadmap / backlog (not yet built)

*(Consolidated from the full Idea 5 feature list in `send-idea-full-detail.md` — narrow down as the audit and scoping conversation happen. Don't attempt to build all of it at once.)*
